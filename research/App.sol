
pragma solidity ^0.4.14;

/* contract App {
    Registry registry; 

    // get an Instance of the Registry so you can write to the instance variables of that contract
    function App (Registry registryAddress) public {
        registry = registryAddress; 
    }

    struct App {
        uint index; 
        // Creation of App either in Organization or User 
        // If it is Organization, which in fact is just controlled by the 
        address owner; 
        address[] versions;
        mapping(uint => Version) version; 
    } 

    struct Version{
        bool owner;
        uint index; 
    }

    // creating means to register ETHR Address of that identity 
    function createApp(address organizationAddress, address appAddress) 
        
    }



}  */